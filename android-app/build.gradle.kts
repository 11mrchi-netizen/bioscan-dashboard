// Top-level build file. Versions below were current at the time this scaffold
// was written (see mobile-app-implementation-roadmap.md Step 1) -- Android
// tooling recommendations shift, so let Android Studio's own upgrade
// assistant/AGP prompts bump these rather than treating them as pinned forever.
plugins {
    id("com.android.application") version "8.6.0" apply false
    id("org.jetbrains.kotlin.android") version "2.0.20" apply false
    id("org.jetbrains.kotlin.plugin.compose") version "2.0.20" apply false
}
