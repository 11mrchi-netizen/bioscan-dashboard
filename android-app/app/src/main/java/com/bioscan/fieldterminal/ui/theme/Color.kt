package com.bioscan.fieldterminal.ui.theme

import androidx.compose.ui.graphics.Color

// Placeholder only. Real design tokens (ground/panel/ink/amber/etc.) come from
// /design/README.md's Field Terminal token table once Phase B (Step 4) applies
// them across the whole app -- not duplicated here yet to avoid drifting from
// that single source of truth.
val PlaceholderBackground = Color(0xFF14161A)
val PlaceholderInk = Color(0xFFE9EDF2)
