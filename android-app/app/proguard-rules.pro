# No rules yet -- release build isn't minified (see app/build.gradle.kts).
# Add Supabase/Ktor/kotlinx.serialization keep rules here if minification is
# turned on later; both are known to need them.
